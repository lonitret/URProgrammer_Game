using UnityEngine;

public enum SoundType
{
    // Фон
    BackgroundMusic,
    OfficeAmbient,

    // SFX
    CoffeeMachine,
    NPCVoice,
    Repair,

    // Высокий гнев
    Heartbeat,
    LowRumble,

    // UI и события
    UIClick,
    MinigameTick,
    Positive,
    LevelComplete,
    GameOver,
    ErrorDecline
}
